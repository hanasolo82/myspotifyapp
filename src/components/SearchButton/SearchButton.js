import React from 'react';
import styles from '../SearchButton/SearchButton.module.css';


function SearchButton() {
    return (
        
            <button className={styles.searchBtn}>button Search</button>
        
    );
};

export default SearchButton;
